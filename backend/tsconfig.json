{
  "compilerOptions": {
    /* Basic Options */
    "target": "ES2022",                // Support modern JS features
    "module": "NodeNext",              // Required for proper ESM (import/export)
    "moduleResolution": "NodeNext",    // Ensures .js extensions in imports work
    "rootDir": "..",                   // Where your code lives, one level up to include shared (a quick and dirty solution to removed IDE warnings)
    "outDir": "./dist",                // Where compiled code will go
    
    /* Strictness & Safety */
    "strict": true,                    // Keeps your code high quality
    "esModuleInterop": true,           // Fixes compatibility with older libraries
    "skipLibCheck": true,              // Speeds up compilation
    "forceConsistentCasingInFileNames": true
  },
  "include": [
    "src/**/*",
    "../shared/**/*"
  ]
}